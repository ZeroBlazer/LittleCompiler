// b = a + twice(b);

    // if (true) {
    //     bool y = true && false;
    //     print("Hello World!");
    // }

    // if (b < a) {
    //     print("b less than a");
    // }

fn twice(x: int): int {
    x * 2
}

fn main(): int {
    var a: int = 2;
    var b: int = 3 * a;

    twice(b)
}

main();